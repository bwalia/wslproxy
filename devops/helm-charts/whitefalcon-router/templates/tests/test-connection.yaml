apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whitefalcon-router.fullname" . }}-test-connection"
  labels:
    {{- include "whitefalcon-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whitefalcon-router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
