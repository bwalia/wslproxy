apiVersion: v1
kind: Service
metadata:
  name: {{ include "whitefalcon-api.fullname" . }}
  labels:
    {{- include "whitefalcon-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whitefalcon-api.selectorLabels" . | nindent 4 }}