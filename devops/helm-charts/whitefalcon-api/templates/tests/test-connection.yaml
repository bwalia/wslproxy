apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whitefalcon-api.fullname" . }}-test-connection"
  labels:
    {{- include "whitefalcon-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whitefalcon-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
