apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whitefalcon.fullname" . }}-test-connection"
  labels:
    {{- include "whitefalcon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whitefalcon.fullname" . }}:{{ .Values.service.apiPort }}']
  restartPolicy: Never
