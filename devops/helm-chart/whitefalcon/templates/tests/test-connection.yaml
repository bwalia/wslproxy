apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whitefalcon.fullname" . }}-{{ .Values.app.type }}-test-connection"
  labels:
    {{- include "whitefalcon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whitefalcon.fullname" . }}-{{ .Values.app.type }}:{{ .Values.service.port }}']
  restartPolicy: Never
