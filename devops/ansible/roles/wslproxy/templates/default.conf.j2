    # Default server configuration
    # server_name is configured in the main nginx.conf.j2 template
    # using the nginx_default_server_hostname variable
    # Current value: {{ nginx_default_server_hostname }}
