apiVersion: v1
kind: Service
metadata:
  name: {{ include "openresty-demo.fullname" . }}
  labels:
    {{- include "openresty-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openresty-demo.selectorLabels" . | nindent 4 }}
