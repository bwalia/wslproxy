apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openresty-demo.fullname" . }}-test-connection"
  labels:
    {{- include "openresty-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openresty-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
